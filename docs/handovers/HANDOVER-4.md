# Handover 4 — Phase 3 no-cost track (serving specified, not provisioned)

**Date:** 2026-09-10 · **Covers:** session 6 (continues `HANDOVER-3.md`)
**Status: everything that costs $0 is done. Total spend CAD $0.00 / $50.00.
The only Phase 3 remainder is the Fabric trial + dashboard build — a CEO
cost decision that must land BEFORE `fabric-architect` provisions anything.**
**Branch state:** uncommitted work on top of `claude/phase-2-pipeline-gate-2`
(`9287969`, pushed; PR opening still a human step) · pytest 30/30 · ruff clean.

Detail lives elsewhere and is not repeated here:
`docs/decisions/0008-deferred-fabric-serving.md` (the deferral ruling) ·
`docs/data-dictionary.md` (tables, columns, measures M1–M8 + pending P1–P3) ·
`docs/measure-reconciliation.json` (pinned values, regenerated by script) ·
`docs/report-plan.md` (5 pages) · `docs/fabric-setup.md` (DRAFT, UNPROVISIONED).

---

## 1. What this session shipped (all $0)

| Artifact | What it is | Verified how |
|---|---|---|
| `scripts/export_gold_parquet.py` + `make export-gold` | Gold → `data/gold-parquet/` (7 files, 1:1 tables). THE serving contract: a future session uploads bytes, transforms nothing | Ran live: 6,940 / 5 / 89 / 6 / 15 / 827,919 / 21,658 rows, matching warehouse |
| `scripts/reconcile_measures.py` + `make reconcile` | Runs measure library M1–M8, writes `docs/measure-reconciliation.json` (committed). A dashboard number disagreeing with that file is wrong until proven otherwise | Ran live; JSON pinned at git `9287969` |
| `docs/measure-reconciliation.json` | Pinned values incl. velocity medians (TOR 28d / MISS 34d / BRAM 44d applied→issued; MISS 385d submitted→decision) + 4 pending measures with reasons + owners | `tests/unit/test_measure_reconciliation.py` (4 contract tests) guards shape in CI |
| `docs/data-dictionary.md` | Every gold table/column + M1–M8 definitions with gold SQL + DAX sketches + PL-300 mapping | M3 active-app arithmetic verified by query (TOR 1,694 / BRAM 719 / MISS 255 — APPEALED is non-terminal) |
| `docs/report-plan.md` | 5 pages with denominators, empty states (Caledon no-data, P1–P3 pendings, TOR-permit map exclusion), ward-drill rule | Reviewed against director-analytics checklist intent |
| `docs/fabric-setup.md` | OneLake convention, 1:1 mapping, publish path, refresh strategy, execution checklist — bannered DRAFT/UNPROVISIONED, screenshots owed | Spec only; nothing clicked |
| `docs/decisions/0008` | Deferral ruling: parquet export is the contract, DuckDB stays source of truth, Gate 3 partial expected | Accepted, consequential |

## 2. What each agent does next (read this before touching Phase 3)

- **`fabric-architect`: STOP at `fabric-setup.md` §4.** Do not provision,
  trial, or click anything until the CEO logs a cost decision in
  `docs/cost-log.md`. Your entire input pack is ready (§1 rows 1, 4, 6, 7).
  If the trial lapses later, serving collapses to the local path — zero rework.
- **`semantic-model-designer`:** dictionary is done; on provisioning, build
  relationships (single filter direction), mark dim_date, implement M1–M8 in
  DAX, and confirm each value against the reconciliation JSON.
- **`report-builder`:** build exactly `report-plan.md`, page 5 included. No
  pending measure as zero, ever. Caledon gets the no-data state, not a blank.
- **`data-quality-auditor`:** two packs await review — the Gate 2 pack
  (`docs/gates/gate-2.md`) and this session's measure pack. Veto reserved on both.
- **`cost-controller`:** serving spend CAD $0.00. Log the trial decision + date
  before any provisioning click.
- **`release-manager`:** Gate 3 will be a scoped partial at most (dashboard-URL
  criterion needs the trial). Record exactly one outstanding item when the time comes.

## 3. Wiring points for the paid components (closer to the end)

1. CEO approves Fabric trial → log in `cost-log.md` → `fabric-architect`
   executes `fabric-setup.md` §4 checklist (upload `data/gold-parquet/` →
   lakehouse → SQL endpoint → semantic model → report → public URL).
2. Re-run `make reconcile` post-refresh as the publish gate (±20% guard fails
   the publish, dashboard keeps last good refresh with its date).
3. Record the public dashboard URL in `fabric-setup.md`, README, and Gate 3 file.
4. Phase 4 needs (unchanged, still later): Anthropic API key for the query
   layer + eval harness; embeddings compute is local (`sentence-transformers`).

## 4. Open items (canonical list: `OUTSTANDING.md` — read that, not this section)

`OUTSTANDING.md` (repo root) is the single leftover list: human steps (§1),
funding gates (§2), technical work with owners (§3), gate status (§4). Summary:

StatCan series HTTP 406 (P1/P3, ingestion-engineer) · Brampton ward
equivalence UNCONFIRMED (report footnote, transform-engineer) · app→permit
timing needs a proven key (P2, ADR-0006) · Gate 2 auditor sign-off + PR
merge (human steps, HANDOVER-3 §4).

## Rules that do not bend (restated, still enforced)

Every published number comes from a real run with a committed script and output.
Evidence is a file path or command output, never an assertion. No secrets, ever.
No agent marks its own work done. No paid resource without a CEO decision in the cost log.
