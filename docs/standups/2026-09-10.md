## Standup — 2026-09-10 — Phase 1

**Shipped:**
- `docs/build-plan.md` — the plan committed as the project's source of truth
- `docs/charter.md` — Gate 0 closed, all four charter questions answered by the CEO
- `.claude/agents/` — 26 agent definitions, one per role card; verified all present, names
  matching, all seven required sections in each
- `pyproject.toml`, `uv.lock`, `Makefile`, `.pre-commit-config.yaml`, `.gitignore`,
  `commitlint.config.js` — 114 packages resolved and locked on Python 3.11
- `.github/workflows/` — five workflows: `pr`, `pipeline`, `verify-sources`, `web`,
  `source-discovery`
- `LICENSE` (MIT), `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, issue templates,
  PR template with every Director checklist, `.github/labels.yml`, `docs/repo-metadata.md`
- `README.md` — rewritten as the front door; no numbers, no dead links, the web app framed
  as a thin client
- `docs/design-plan.md` — design-lead Pass 1 and the Pass 2 self-critique
- `config/sources.yml`, `scripts/verify_sources.py`, `scripts/discover_sources.py`,
  `docs/sources/*`
- `docs/decisions/0001-tooling-and-ci.md`, `docs/decisions/0002-org-reporting-lines-and-review-authority.md`
- `docs/cost-log.md`, `docs/handovers/HANDOVER-1.md`
- 42 GitHub issues: six gate trackers and 36 work items mirroring the phase plan

**In flight:**
- `transform-engineer` — `docs/schema-design.md`, the star schema drafted on paper against
  unverified sources, with every assumption flagged as falsifiable
- `director-data-engineering` — has not yet reviewed the schema draft
- `director-product-frontend` — has not yet approved the design plan; all UI code is blocked
  behind that approval

**Blocked:**
- **Source verification cannot run in the build environment.** Its egress allowlist rejects
  every municipal, ArcGIS and StatCan host. Confirmed twice independently, including a
  control test where unrelated hosts failed identically while github.com succeeded.
  *Owner:* resolved in workaround — `verify-sources.yml` and `source-discovery.yml` now run
  the checks on GitHub runners, which have normal internet access. Not a CEO decision.
- **The Toronto development applications licence is `License not specified`.** That dataset
  is the primary feed for `fct_applications`, and the project rule is that an unconfirmed
  licence blocks public use exactly like a restrictive one.
  *Owner:* `source-scout` and `security-and-licence-reviewer`. **CEO decision may be needed**
  if the portal-level licence does not cover it.
- **Milestones and branch protection are not automatable here.** No milestone-creation tool
  exists in the available tooling; branch protection is not settable from repository files.
  *Owner:* CEO, manually. Steps are in `docs/repo-metadata.md`.

**Numbers:** all measured this session, from live responses on a GitHub runner.
- Toronto `development-applications`: **26,613 rows**, 25 columns, refreshed daily, last
  refreshed 2026-09-10 07:26:09
- Toronto `city-wards`: **25 rows**, 20 columns, refreshed semi-annually
- Peel `MunicipalBoundary_Peel`: **3 rows** — Mississauga, Brampton, Caledon
- Peel `Lot Fabric Improved`: **1,841 rows**
- StatCan: **8,270 cubes** listed; six product ids confirmed relevant, including 34100292
  (building permits, active) and 34100143 (CMHC housing starts, active)
- Source liveness: **10 of 10 registered hosts returned HTTP 200** from a runner
- Dependencies: **114 packages** resolved and locked
- GitHub issues: **42** open
- Cost to date: **CAD $0.00** against a CAD $50.00 ceiling
- Eval scores, gold row counts, dashboard figures: **not yet measured** — nothing built

**Next session:**
1. Re-run discovery for Toronto building permits, Mississauga and Brampton layers, and
   Peel's `Building_Permits` service; transcribe real ids, counts and fields into
   `config/sources.yml` and calibrate `expected_min_rows`.
2. Resolve the Toronto applications licence, and every other source licence. Nothing is
   ingested into a public artifact before its licence is named.
3. Get `docs/schema-design.md` reviewed by `director-data-engineering` and
   `docs/design-plan.md` approved by `director-product-frontend`, then have
   `release-manager` write `docs/gates/gate-1.md`.

---

## Standup — 2026-09-10 (session 2, local verification) — Phase 1 → Gate 1

**Shipped:**
- `config/sources.yml` — transcribed 26 entries with live `observed_rows`,
  calibrated `expected_min_rows`, named licences; `verify_sources.py` → 25/25, exit 0
- `docs/sources/evidence/2026-09-10-local-verification.md` — every number from a live call
- `tests/unit/test_sources_registry.py` — `_DEV` guard + verified-live evidence guards (3 passed)
- Per-source docs updated (Toronto/Mississauga/Brampton/Peel/StatCan) + sources README index
- `docs/reviews/director-data-engineering-schema-design.md` — CONDITIONAL PASS, 6 rulings
- `docs/reviews/director-product-frontend-design-plan.md` — APPROVED with Caledon-map condition
- `docs/gates/gate-1.md` — CONDITIONAL PASS (release-manager)

**Decisions closed:** Brampton production = `maps1.brampton.ca` MapServer (222,263 rows,
current), `_DEV` frozen 2018 (rejected, test-guarded); all 6 licences named
(Toronto OGL-Toronto, Mississauga Terms of Use, Brampton CC BY, Peel Licence v1.0,
StatCan Open Licence); CMHC NOT ADOPTED (data via StatCan); Peel service name
corrected (`Municipal_Boundary`); Peel ward PII flagged for silver-drop.

**In flight:** 5 ADRs owed at Phase 2 start; Toronto 32-col field capture;
CEO-manual repo settings; branch merge → re-drill.

**Blocked:**
- Cold clone drill FAILS on `main` (empty; work unmerged) — needs PR merge, then re-drill.
- Milestones/branch protection/labels/topics need CEO (manual, `docs/repo-metadata.md`).
- Caledon permit feed: none exists — scope decision owed Phase 2.

**Numbers:** Toronto active 206,259 · cleared 435,942 · apps 26,613 · wards 25;
Mississauga permits 34,615; Brampton prod permits 222,263 (DEV 141,886 frozen);
Peel boundary 3 · wards 27/26 · CT census 282; StatCan 8,270 cubes.
Cost to date: **CAD $0.00** vs CAD $50 ceiling. Eval/gold/dashboard: not yet measured.

**Next session:** (1) CEO: milestones/protection/labels/topics + accept Gate 1
CONDITIONAL PASS; (2) merge branch to `main`, re-run cold clone drill;
(3) write the 5 Phase-2-entry ADRs; (4) capture Toronto permit field lists,
then Phase 2 ingestion begins.

---

## Standup — 2026-09-10 (session 3, GitHub closeout) — Gate 1 PASS

**Shipped (all via API + git, verified, no UI clicks):**
- Committed session work in 4 scoped commits; pushed session branch
- Milestones #1–#6 (Gates 0–5; Gate 0 closed) — created
- 19 labels synced from `.github/labels.yml` (18 updated, 1 created)
- 10 repo topics set (`docs/repo-metadata.md`)
- Branch protection on `main`: PR + 1 review + conversation resolution + strict
  status checks (6 `pr.yml`/`web.yml` contexts; schedule-only verify-sources excluded)
- PR #43 opened (Director checklists in body) → CI red on `ruff format` →
  fixed, pushed → CI all green → merged (`efabb92`)
- Cold clone drill re-run on `main`: PASS (full tree, verify 25/25, pytest 3/3)
- `docs/gates/gate-1.md` promoted CONDITIONAL PASS → PASS; closeout in PR #44

**Blocked:** nothing. One gap found in CI config honesty: `pr.yml` lint job runs
`ruff check` + `ruff format --check` — both now proven green on this tree.

**Numbers:** cost still CAD $0.00. 42 issues open, 6 milestones, PRs #43 merged.

**Next session:** Phase 2 entry — (1) 5 ADRs (unit-basis, Peel framing, app grain,
linkage, ward versioning); (2) Toronto permit 32-column field capture;
(3) ingestion connectors with PII-drop + key-stability tests.

---

## Standup — 2026-09-10 (session 4, Phase 2 entry + bronze) — Gate 2 open

**Shipped:**
- `docs/decisions/0003–0007` — the five Phase-2-entry ADRs (unit-basis,
  Peel framing + Caledon scope, app grain, real-key-only linkage, ward versioning)
- `docs/sources/evidence/2026-09-10-phase2-field-capture.md` — live schemas
  for every feed (§§1–8) + key-resolution findings (§§10–12)
- `src/ingest/` (9 modules) + `config/ingest.yml` (20 feeds) — CKAN /
  OID-range ArcGIS / StatCan-metadata connectors, checkpointed,
  drift-detecting, key-checked, manifest per run
- `tests/unit/test_{pii_columns,key_stability,ingest_spec}.py` — 16 new tests
- `Makefile ingest` wired to `uv run python -m src.ingest --all`
- Bronze: **20/20 feeds `ok`, exit 0, twice consecutively byte-identical**
  (~940k rows; counts equal reported totals on every feed)

**Decisions closed:** composite row keys proven per feed (Toronto triple+_id,
Brampton folder+row, double-publication composites); Toronto app grain is
per-address (dedup rule in ADR-0005); Peel permits = aggregate stats table;
no-resume same-date-replace rule; StatCan series unproven (406, specs stay empty).

**In flight:** silver/gold SQL, conformance matrix, `tests/data/`,
data-quality report — untouched, next session.

**Blocked:** nothing. StatCan 406 and the CMA-level geography are recorded
open items, not blockers (reconciliation is a data-quality-time concern).

**Numbers:** bronze ~940k rows across 20 feeds (see HANDOVER-3 §2 for the
split); pytest 19/19; ruff clean; cost **CAD $0.00** vs CAD $50 ceiling.
Eval/gold/dashboard: not yet measured.

**Next session:** (1) commit this session as scoped PR(s); (2) Gate 2 file
with auditor sign-off; (3) Phase 3 serving/BI scoping.

---

## Standup — 2026-09-10 (session 4b, transform) — Gate 2 ready, sign-off owed

**Shipped:**
- `models/silver/00–09` + `models/gold/00–06` — hand-written SQL, grain headers
- `src/transform/` (runner + CLI: `--all`, `--report`) — `make transform`, `make report` wired
- `docs/conformance-matrix.md` — full crosswalks from live vocabularies
- `tests/data/` — invariants + 6 fixture tests (CI-safe) + live warehouse test
- `docs/data-quality-report.md` — generated from the build, caveats stated
- Warehouse: fct_permits **827,919** · fct_applications **21,658** · 0 orphans

**Fixed live:** Mississauga ward fan-out (vintage-pinned joins); future/negative
reclassified as monitored-with-explanation; parquet page-schema variance.

**Blocked:** nothing technical. Gate 2 verdict + auditor sign-off owed next session.

**Numbers:** pytest 26/26; ruff clean; cost **CAD $0.00** vs CAD $50 ceiling.

---

## Standup — 2026-09-10 (session 5, Gate 2 closeout) — Phase 2 → Gate 2

**Shipped:**
- `tests/data/test_transform_logic.py` — lint-fixed (`ruff check` clean,
  `ruff format` applied, pytest 26/26)
- `Makefile` — real `pipeline: ingest transform` target (Gate 2 criterion
  now literally true, not "equivalent")
- `docs/gates/gate-2.md` — CONDITIONAL PASS draft, evidence inline per
  criterion, auditor sign-off as the single condition
- `docs/handovers/HANDOVER-3.md` — status updated to session-5 closeout
- Transform rebuilt from landed bronze: fct_permits **827,919** ·
  fct_applications **21,658** · 0 orphan FKs (identical across rebuilds)

**In flight:** session branch with 4 scoped commits (entry ADRs+evidence /
bronze / transform / gates+handovers) — pushed to origin, PR opening is a
human step (no `gh` in this environment).

**Blocked:** nothing technical. Human steps owed: open PR → 1 review →
merge; `data-quality-auditor` review of the Gate 2 pack (only the CEO can
override a veto).

**Numbers:** pytest 26/26; `ruff check` + `format --check` clean; cost
still **CAD $0.00** vs CAD $50 ceiling. Eval/gold deltas/dashboard: gold
stable at 827,919 / 21,658; dashboard not yet started (Phase 3).

**Next session:** (1) PR merged → cold-clone-style verify on `main`
(`verify_sources` + pytest); (2) auditor sign-off → Gate 2 PASS;
(3) Phase 3 kickoff — Fabric trial is a CEO cost decision BEFORE
`fabric-architect` provisions anything; `semantic-model-designer` data
dictionary can start from gold + conformance matrix without the trial.

---

## Standup — 2026-09-10 (session 6, Phase 3 no-cost track) — serving ready, trial owed

**Shipped (all $0, all verified live):**
- `scripts/export_gold_parquet.py` + `make export-gold` — 7 gold tables →
  `data/gold-parquet/`, counts match warehouse (the serving contract)
- `scripts/reconcile_measures.py` + `make reconcile` →
  `docs/measure-reconciliation.json` (committed, pinned at `9287969`)
- `docs/data-dictionary.md` — tables, columns, M1–M8 (SQL + DAX sketches) +
  P1–P3 pending with owners + PL-300 mapping
- `docs/report-plan.md` — 5 pages with denominators + mandated empty states
- `docs/fabric-setup.md` — DRAFT/UNPROVISIONED spec + execution checklist
- `docs/decisions/0008-deferred-fabric-serving.md` — deferral ruling
- `docs/handovers/HANDOVER-4.md` — per-agent next actions + paid wiring points
- `tests/unit/test_measure_reconciliation.py` — 4 contract tests

**Fixed live:** M3 arithmetic corrected by query (APPEALED non-terminal:
TOR 1,694 / BRAM 719 / MISS 255); ruff UP017; my own `ruff` invocation
briefly linted the Makefile as Python — operator error, not a repo issue
(CI runs `ruff check .`, Python files only).

**Blocked:** nothing technical. Paid remainder: Fabric trial (CEO cost
decision first) → provision → dashboard → Gate 3. Human steps still owed:
PR open → review → merge; Gate 2 + measure-pack auditor review.

**Numbers:** pytest 30/30; `ruff check` + `format --check` clean; gold
stable 827,919 / 21,658; velocity medians TOR 28d / MISS 34d / BRAM 44d,
MISS submitted→decision 385d; cost **CAD $0.00** vs CAD $50 ceiling.

**Next session:** (1) open PR for `claude/phase-2-pipeline-gate-2` (+ this
session's work — decide one PR or two); (2) merge → verify on `main`;
(3) auditor sign-offs; (4) CEO trial decision when ready — everything downstream
is specified and waiting.

---

## Standup — 2026-09-10 (session 7, closeout) — all committed, demo ran

**Shipped:**
- `OUTSTANDING.md` — canonical leftover list (human steps / funding /
  technical + owners / gate status)
- `docs/handovers/HANDOVER-4.md` §4 — now points at `OUTSTANDING.md`
- Session-6 pack + this closeout committed on `claude/phase-2-pipeline-gate-2`
  and pushed; tree clean
- Live demo: `make transform` → export → reconcile + gold showcase queries
  (see CEO brief — real numbers below)

**Blocked:** nothing technical. Awaiting humans: PR → merge → auditor
sign-offs → trial decision (all in `OUTSTANDING.md` §1–2).

**Numbers:** pytest 30/30; ruff clean; cost **CAD $0.00** vs CAD $50 ceiling.

**Next session:** whatever `OUTSTANDING.md` says is next — PR merge first.

---

## Standup — 2026-09-10 (session 9, web scaffold) — site builds, numbers real

**Shipped (all $0):**
- `web/` — Next.js 15 static export, 6 routes per plan wireframes, design
  tokens + dark mode + zero animations, Google Fonts with system fallbacks
- `scripts/build_map_svg.py` → `web/public/gta-map.svg` — real bronze
  geometry (Peel 3857 rings + Toronto ward polygons), fills by gold volume,
  Caledon no-data state; regen byte-identical (SHA256)
- `web/scripts/a11y-check.mjs` — 7/7 pages pass (h1, lang, captions, skip
  link, map role)
- Reconcile `bronze` + `integrity` sections → landing status table (20/20
  feeds) + data-quality page (0 orphans) read committed JSON only
- `docs/decisions/0009-static-export-web.md` + `docs/handovers/HANDOVER-5.md`
- One rule conflict resolved for the record: jsx-a11y vs plan §7 scroll
  regions — plan wins, documented in code

**Verified live:** `npm run lint` clean, `next build` 9 static pages,
`test:a11y` green; built HTML spot-checked (827,919 / 21,658 / 20-feed
table / map titles / 0 orphans / pending owners). Python: ruff clean,
pytest 32/32. First load JS 103 kB.

**Blocked:** nothing technical. Commit decision owed (suggest one `feat(web)`
commit on a fresh branch → PR → `web.yml` CI → merge). Then: deploy preview
(static host, URL at Gate 4b — no subdomain), human Lighthouse pass.

**Numbers:** cost **CAD $0.00** vs CAD $50 ceiling. No pipeline values changed.

**Next session:** commit → PR → merge → deploy preview; funded backends
(Fabric trial, API key, eval harness) per `OUTSTANDING.md` §2 when ready.

---

## Standup — 2026-09-10 (session 10, commit + local host) — site serves

**Shipped:**
- Branch `claude/web-scaffold-static-export`, one commit (`3ee5d73`,
  `feat(web)`), pushed. Tree clean.
- Local host: `serve` on http://localhost:8123 serving `web/out/`
  (left running; stop with `Stop-Process -Id 22020`).
- Hosted tests (fresh script, 8/8 green): all 6 routes 200 with content
  markers (827,919 / 21,658 / map role / empty-state texts), map SVG 200,
  unknown route 404.

**Blocked:** nothing technical. PR opening → review → merge is human work
(`OUTSTANDING.md` §1 has the link).

**Numbers:** pytest 32/32; npm lint/build/a11y green pre-commit; cost
**CAD $0.00** vs CAD $50 ceiling.

**Next session:** PR → merge → deploy preview + Lighthouse; funded backends
per `OUTSTANDING.md` §2 when ready.

---

## Standup — 2026-09-10 (session 12, PR #47 merged) — main verified

**Verified against `main` (`1116b17`) + GitHub API:**
- Merge commit is the PR #47 merge; tree holds all 26 files (+6,222/−9).
  Local `main` fast-forwarded, clean.
- Pre-merge CI on head `f983fdb`: all 4 runs green — PR workflow and Web
  workflow (lint, build, a11y) both `success`. The eslint fix worked in CI.
- Local re-run on merged tree: pytest 32/32, ruff check + format clean.
- The merged branch still exists remote (cleanup not requested this round).

**Blocked:** nothing. Next human steps unchanged (`OUTSTANDING.md` §1):
deploy preview, Lighthouse pass, auditor sign-offs, trial decision.

**Numbers:** cost **CAD $0.00** vs CAD $50 ceiling.

---

## Standup — 2026-09-10 (session 13, skill audit + hygiene)

**Skill (ui-ux-pro-max) verdict:** ran the full workflow (design-system +
ux + nextjs searches). Rejected the art direction (generic SaaS look
vs approved municipal-register identity — plan outranks database).
Applied the universal rules: 44px touch targets, hover feedback,
dead-CSS removal, viewport-meta assertions in `test:a11y`. Deliberately
not applied (plan governs): smooth-scroll (harms skip-link users), body
16px/65ch (plan tokens: 15px UI, 45rem prose), Fira/blue-orange restyle,
animations/tooltips/spinners (plan: zero motion).

**Hygiene:** `claude/web-scaffold-static-export` deleted (merged) — only
`main` local + remote. 42 issues audited: 19 closable (#1, #7–#22, #25,
#27 — all done with evidence), 23 stay open (see HANDOVER-6 §4).

**Auth boundary:** no non-interactive GitHub API auth here (no gh/token;
credential fill empty). Push + public reads work; PR opens + issue closes
are handed over with bodies/lists ready.

**Verified:** `npm run lint/build/test:a11y` green (7 pages incl. new
viewport check); pytest 32/32; ruff clean.

**Numbers:** cost **CAD $0.00**. No pipeline values changed.

**Next:** branch → commit → push this pack; human opens PR + closes the 19.

---

## Standup — 2026-09-10 (session 11, PR #47 web-job red → green)

**Diagnosis (again not merge conflicts):** the web job failed at its Lint
step on `web/next-env.d.ts:3` — a `/// <reference path>` line that `next
build` appends itself. Local lint passed because it ran before the final
local build regenerated the file; the committed copy post-dated it. Same
bug class as session 8 (unverified artifact committed).

**Fix:** `next-env.d.ts` added to eslint ignores (generated file — never
linted, never hand-edited). Verified `npm run lint/build/test:a11y` green
on the exact committed file state.

**Numbers:** cost still **CAD $0.00**. No site content changed.

**Next session:** web job should go green → review → merge PR #47.

---

## Standup — 2026-09-10 (session 8, PR #46 CI red → green)

**Diagnosis (not merge conflicts):** PR #46 API state is `mergeable: true`,
`mergeable_state: unstable` — the merge itself is clean (verified by local
`merge-tree`: empty). The red is three CI jobs: unit tests + data tests
(exit 2, collection errors) and mypy (exit 1). Lint and eval were already green.

**Root causes, all local-environment blind spots:**
1. `import src` worked under system `python -m pytest` (cwd on sys.path)
   but fails under `uv run pytest` (clean venv) — 5 collection errors.
   Fix: `pythonpath = ["."]` in `[tool.pytest.ini_options]`.
2. `mypy strict` was never run locally (no mypy here): `fetchone()[0]` on
   an Optional (runner.py ×2) + a union assignment (`__main__.py`).
   Fix: assert-narrowing `scalar()` + explicit `CkanPull | ArcgisPull`.
3. Bonus honesty fix: `-m unit` / `-m data` selected ZERO tests (no marks
   applied anywhere) — CI was passing vacuously. All 7 test modules now
   carry `pytestmark`, so CI executes 23 unit + 7 data tests for real.

**Verification:** installed `uv` + Python 3.11 locally (`uv sync --extra dev`,
114 packages, lock consistent) and ran the exact CI commands:
`ruff check` + `format --check` clean, `mypy src` clean (14 files),
`-m unit` 23 passed, `-m data` 7 passed, full suite 30/30 on both interpreters.

**Numbers:** cost still **CAD $0.00**. No pipeline values changed (code-only fix).

**Next session:** PR #46 should go green on re-run → review → merge → verify
on `main`. Fill the PR body's Evidence section with the CI run link.

---

## Standup — 2026-09-10 (session 14, Gate 2 PASS) — CEO approved

**Answered:** no human quality auditor was ever assigned — the
`data-quality-auditor` is an agent role card, its sign-off slots read
"owed"/"not yet run" in both gate files. CEO reviewed the pack and approved
Gate 2 directly under build-plan §4 ("Only the CEO can override").

**Shipped:** `docs/gates/gate-2.md` promoted CONDITIONAL PASS → PASS
(criterion 7 rewritten with the §4 basis, criterion 3 refreshed to 32/32
re-verified on merged `main`, outstanding items marked cleared/carried,
CEO sign-off recorded). `OUTSTANDING.md` gate table + §1 updated.

**Process note:** first attempt cut this branch from the unmerged UI branch
by mistake — caught before pushing (log check), rebuilt cleanly from `main`
(`1116b17`). Lesson: check `git log` before branching, every time.

**Blocked:** nothing. Measure-pack review still open (fold into next human
review or CEO-accept with Gate 3).

**Numbers:** pytest 32/32 re-verified; cost **CAD $0.00**.

**Next:** PR with the gate file → merge → close Gate 2 issue #2 (human
write access needed).

---

## Standup — 2026-09-10 (session 15, Gate 2 merged) — verified on `main`

**Verified:** PR #48 merged (`1c89893`); local `main` fast-forwarded, clean.
Gate file on `main` reads PASS with CEO sign-off. Merged branch
`chore/gate-2-pass` deleted local + remote.

**Left on this end (human work only):** close Gate 2 issue #2; open → merge
the ui-ux-pass PR (`claude/ui-ux-pass-touch-a11y`, pushed, unmerged); close
the other 18 done issues; measure-pack review; deploy preview + Lighthouse +
ninety-second test; cold-clone verify. Funded gates (trial, API key, domain)
per `OUTSTANDING.md` §2.

**Numbers:** cost **CAD $0.00** vs CAD $50 ceiling.

---

## Standup — 2026-09-11 (session 16, PR #49 + Fabric kickoff)

**Shipped:** PR #49 merged (`fb02c8e`), branch deleted — only `main`
remains, clean. School-account Fabric replaces the trial plan: decision
logged in `cost-log.md` ($0.00 personal), free-tier inventory updated,
`docs/fabric-dax.md` written (M1–M8 + relationship map + first-refresh
verification rule), HANDOVER-7 with the tenant capability checklist +
execution order. `OUTSTANDING.md` reconciled (stale bullets fixed).

**Blocked:** portal clicks + capability answers need the human (HANDOVER-7
§2: capacity, publish-to-web, separation, upload). Nothing provisioned.

**Numbers:** pytest 32/32 (unchanged tree); cost **CAD $0.00**.

**Next:** capability answers → provision per HANDOVER-7 §3 → Gate 3 scoped
file once the dashboard URL exists.

---

## Standup — 2026-09-11 (session 17, Pro workspace picked)

**Tenant evidence:** workspace creation offers Power BI Pro / PPU only — no
Fabric capacity, no trial. Decision: **Pro** (ADR-0010). PPU's extras (48
refreshes/day, >1 GB models, XMLA) buy nothing at ~830k rows with weekly
refreshes, and PPU narrows the viewer pool. Lakehouse sections of
`fabric-setup.md` demoted to upgrade path; serving v1 is Desktop → Pro
workspace, manual refresh + reconcile gate.

**Shipped:** ADR-0010, `fabric-setup.md` status note, `OUTSTANDING.md` §2
rewrite. `fabric-dax.md` applies unchanged (relationships + M1–M8).

**Blocked:** human clicks — create `gta-housing` workspace, install Power BI
Desktop, import parquet, model, verify vs JSON, 5 pages, publish. Then the
publish-to-web test decides the `/dashboard` embed.

**Numbers:** cost **CAD $0.00**. No code touched.

**Next:** workspace created → report back; then model build with DAX file
in hand.
